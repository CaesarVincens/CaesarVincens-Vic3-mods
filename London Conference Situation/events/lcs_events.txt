namespace = lcs_error_suppression

lcs_error_suppression.0001 = {
	hidden = yes
	orphan = yes
	trigger = {
		exists = global_var:lcs_is_loaded
	}
}

lcs_error_suppression.0002 = {
	hidden = yes
	#orphan = yes
	trigger = {
		#exists = global_var:lcs_is_loaded
	}
	immediate = {
		set_global_variable = lcs_is_loaded
	}
}